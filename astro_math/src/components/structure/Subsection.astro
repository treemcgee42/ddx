---
interface Props {
  title: string;
}

const { title } = Astro.props;

import { incrementAndGetCounter } from "./section_counter";
--- 
<h3 class="text-xl font-bold my-4">{incrementAndGetCounter(2) + ". " + title}</h3>
