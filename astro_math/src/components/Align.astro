---
import DisplayMath from "./DisplayMath.astro";

interface Props {
  str: string;
}

const { str } = Astro.props;
---

<DisplayMath
  str={String.raw`\begin{align*}` + str + String.raw`\end{align*}`}
/>
